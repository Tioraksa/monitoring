CREATE TABLE tb_actualdailyengine (
    id SERIAL PRIMARY KEY,
    casemark VARCHAR(50) NOT NULL,
    tgl DATE NOT NULL DEFAULT CURRENT_DATE
);

CREATE TABLE tb_detailprod (
    id SERIAL PRIMARY KEY,
    line VARCHAR(20) NOT NULL,
    lot VARCHAR(50) NOT NULL,
    case_no VARCHAR(50) NOT NULL,
    tgll DATE NOT NULL DEFAULT CURRENT_DATE,
    jam TIME NOT NULL DEFAULT CURRENT_TIME
);

CREATE TABLE tb_prosesplan (
    id SERIAL PRIMARY KEY,
    tgl DATE NOT NULL DEFAULT CURRENT_DATE,
    plan INTEGER NOT NULL,
    ct INTEGER NOT NULL,
    jam_kerja INTEGER NOT NULL
);
ALTER TABLE tb_prosesplan ADD COLUMN line VARCHAR(5) NOT NULL;
